plugins {
    id "org.sonarqube" version "3.3"
	id 'jacoco'
}

repositories {
    mavenCentral()
}

subprojects {
	if (it.name != 'api' && it.name != 'util' && it.name != 'microservices'){
		apply plugin : 'java'
		test
    }
}